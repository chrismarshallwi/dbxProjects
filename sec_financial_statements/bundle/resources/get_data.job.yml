resources:
  jobs:
    get_data:
      name: get_data
      tasks:

        - task_key: get_data
          notebook_task:
            notebook_path: ../../src/sec_quarterly_statements/notebooks/get_data.py
            source: WORKSPACE

      queue:
        enabled: true
      parameters:
        - name: target_catalog
          default: ${var.target_catalog}


      tags:
        data: SEC
      

