# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "Finance"

include:
  - resources/*.yml
  - resources/*/*.yml

variables:
  ops_catalog:
    description: The catalog where assets are created

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-d1866d54-256f.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-d1866d54-256f.cloud.databricks.com
      root_path: /Users/chrismarshall.wi@icloud.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: chrismarshall.wi@icloud.com

  local:
    mode: development
    default: true
    workspace:
      host: https://dbc-d1866d54-256f.cloud.databricks.com
    variables:
      ops_catalog: operations

